import SwiftUI

struct ScheduleView: View {
  var body: some View {
    ZStack {
      Color.vintageGrape.ignoresSafeArea()
      Text("Coming soon")
        .font(.levelBody)
        .foregroundStyle(Color.cream)
    }
  }
}
